import React, {Component} from 'react';

export default class CreateWebsitePassword extends Component {
    constructor(props) {
        super(props);
        // render automatically when modify with states

    }

    render(){
        return <p> this is a test</p>;
    }
}